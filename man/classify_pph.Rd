% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_pph.R
\name{classify_pph}
\alias{classify_pph}
\title{Classify Potentially Preventable Hospitalisations (PPH)}
\usage{
classify_pph(
  df,
  id,
  format = c("long", "wide"),
  df_diag = NULL,
  df_proc = NULL,
  diag = "diag",
  diagno = "diagno",
  proc = "proc",
  diag_prefix = "diag",
  proc_prefix = "proc",
  age_months = "agemonths",
  age_years = "ageyears"
)
}
\arguments{
\item{df}{A data frame with one row per episode.}

\item{id}{The episode identifier column (unquoted name).}

\item{format}{Input format: \code{"long"} (default) or \code{"wide"}.}

\item{df_diag}{Long-format diagnosis data frame. Required when
\code{format = "long"}.}

\item{df_proc}{Long-format procedure data frame. Required when
\code{format = "long"}.}

\item{diag}{Name of the diagnosis code column in \code{df_diag} (quoted string).
Default: \code{"diag"}. Used only when \code{format = "long"}.}

\item{diagno}{Name of the diagnosis sequence number column in \code{df_diag}
(quoted string). Default: \code{"diagno"}. Used only when \code{format = "long"}.}

\item{proc}{Name of the procedure code column in \code{df_proc} (quoted string).
Default: \code{"proc"}. Used only when \code{format = "long"}.}

\item{diag_prefix}{Prefix shared by all diagnosis columns in \code{df} (quoted
string). Default: \code{"diag"}. Used only when \code{format = "wide"}.}

\item{proc_prefix}{Prefix shared by all procedure columns in \code{df} (quoted
string). Default: \code{"proc"}. Used only when \code{format = "wide"}.}

\item{age_months}{Name of the age-in-months column in \code{df} (quoted string).
Default: \code{"agemonths"}.}

\item{age_years}{Name of the age-in-years column in \code{df} (quoted string).
Default: \code{"ageyears"}.}
}
\value{
The input \code{df} with additional columns: \code{PPHnum} (count of PPH
categories identified), and paired \code{PPH1}/\code{PPHcat1}, \code{PPH2}/\code{PPHcat2},
... columns for each identified PPH. Episodes with no PPH assigned have
\code{PPHnum = 0} and \code{PPH1 = PPHcat1 = ""}.
}
\description{
Identifies Potentially Preventable Hospitalisations (PPH) in admitted
patient episodes coded with ICD-10-AM, using the Australian Institute of
Health and Welfare (AIHW) PPH classification.

Both wide-format and long-format inputs are supported via the \code{format}
argument.
}
\examples{
dfbase <- icd10am.utils::dfbase
dfdiag <- icd10am.utils::dfdiag
dfproc <- icd10am.utils::dfproc

# Long format
classify_pph(
  df         = dfbase,
  id         = recordID,
  format     = "long",
  df_diag    = dfdiag,
  df_proc    = dfproc,
  diag       = "diag",
  diagno     = "diagno",
  proc       = "proc",
  age_months = "agemonths",
  age_years  = "ageyears"
)

# Wide format
dfwide <- icd10am.utils::dfwide
classify_pph(
  df          = dfwide,
  id          = recordID,
  format      = "wide",
  diag_prefix = "diag",
  proc_prefix = "proc",
  age_months  = "agemonths",
  age_years   = "ageyears"
)
}
\references{
Australian Institute of Health and Welfare (2021). \emph{Potentially preventable
hospitalisations in Australia by small geographic areas 2017--18}. Cat. no.
HSE 236. Canberra: AIHW.

National Healthcare Agreement: PI 18 -- Selected potentially preventable
hospitalisations (2018). Australian Institute of Health and Welfare.
\url{https://meteor.aihw.gov.au/content/index.phtml/itemId=658499}
}
